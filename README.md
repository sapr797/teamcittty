# TeamCity CI/CD Конфигурация

Проект содержит настройки TeamCity для автоматизированной сборки и деплоя Maven проекта.

## **Задание 4: Условная сборка**

Реализована условная сборка в зависимости от ветки:

### **Логика:**
- **master ветка**: `mvn clean deploy` (деплой в Nexus)
- **другие ветки**: `mvn clean test` (только запуск тестов)

### **Реализация:**
Конфигурация в `.teamcity/pom.kts` содержит два шага с условиями:

```kotlin
// Для master ветки
maven {
    name = "Deploy on master branch"
    conditions {
        or {
            equals("teamcity.build.branch", "master")
            equals("teamcity.build.branch", "refs/heads/master")
            matches("teamcity.build.branch", "")
        }
    }
    goals = "clean deploy"
    mavenSettings = id("Nexus_Settings")
}

// Для других веток
maven {
    name = "Test on other branches"
    conditions {
        and {
            isNotEmpty("teamcity.build.branch")
            doesNotEqual("teamcity.build.branch", "master")
            doesNotEqual("teamcity.build.branch", "refs/heads/master")
        }
    }
    goals = "clean test"
    mavenSettings = id("Nexus_Settings")
}
Задание 5: Настройка подключения к Nexus
Для подключения к Nexus создан файл settings.xml с учетными данными.

Содержимое settings.xml:
xml
<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 
          http://maven.apache.org/xsd/settings-1.2.0.xsd">
    <servers>
        <server>
            <id>nexus-releases</id>
            <username>admin</username>
            <password>admin123</password>
        </server>
        <server>
            <id>nexus-snapshots</id>
            <username>admin</username>
            <password>admin123</password>
        </server>
    </servers>
</settings>
Интеграция в TeamCity:
Файл загружен в TeamCity в раздел "Maven Settings"

Имя конфигурации: Nexus_Settings

В конфигурации сборки добавлена ссылка на настройки

Задание 6: Обновление ссылок в pom.xml
Обновлены URL репозиториев в pom.xml:

Исправления:
distributionManagement - URL для релизов и снапшотов

repositories - URL для загрузки зависимостей

pluginRepositories - URL для загрузки плагинов

Пример исправленных URL:
xml
<distributionManagement>
    <repository>
        <id>nexus-releases</id>
        <url>http://localhost:8081/repository/maven-releases/</url>
    </repository>
    <snapshotRepository>
        <id>nexus-snapshots</id>
        <url>http://localhost:8081/repository/maven-snapshots/</url>
    </snapshotRepository>
</distributionManagement>

<repositories>
    <repository>
        <id>nexus</id>
        <url>http://localhost:8081/repository/maven-public/</url>
        <releases><enabled>true</enabled></releases>
        <snapshots><enabled>true</enabled></snapshots>
    </repository>
</repositories>
Настройка для вашего Nexus:
Замените localhost:8081 на адрес вашего Nexus сервера.

Структура проекта
text
teamcittty/
├── .teamcity/               # Конфигурация TeamCity Kotlin DSL
│   ├── settings.kts        # Главная конфигурация проекта
│   └── pom.kts             # Конфигурации сборок Build и Deploy
├── pom.xml                 # Maven конфигурация с обновленными URL
├── settings.xml            # Учетные данные Nexus
├── src/                    # Исходный код
└── README.md               # Документация
Конфигурация TeamCity
Проекты:
Build - основная сборка с условной логикой

Deploy - развертывание с зависимостью от артефактов Build

Артефакты:
JAR файлы публикуются в TeamCity: target/*.jar => artifacts/

Deploy сборка скачивает артефакты в директорию build-output/

Как развернуть
Создать проект TeamCity из репозитория

Включить Kotlin DSL синхронизацию (путь: .teamcity)

Загрузить settings.xml в TeamCity (Администрирование → Maven Settings)

Запустить сборку Build на ветке master

Примечания
Nexus должен быть доступен по указанному в pom.xml адресу

Учетные данные по умолчанию: admin/admin123
Обзор проекта
Проект представляет собой Java-приложение с использованием Maven, TeamCity для CI/CD, и Nexus как репозиторий артефактов. Реализован процесс автоматической сборки, тестирования и публикации артефактов.

Выполненные задания
🎯 Задание 6: Настройка pom.xml
Цель: Обновить ссылки на репозитории и Nexus в Maven конфигурации.

Выполнено:

В файле pom.xml обновлены репозитории Maven

Настроена секция distributionManagement для публикации артефактов в Nexus:

xml
<distributionManagement>
    <repository>
        <id>nexus</id>
        <url>http://93.77.182.74:8081/repository/maven-releases/</url>
    </repository>
</distributionManagement>
✅ Задание 7: Первая сборка master
Цель: Запустить сборку по ветке master и убедиться в успешной публикации артефактов.

Выполнено:

Сборка запущена в TeamCity

Все этапы сборки пройдены успешно

Артефакты опубликованы в Nexus:

plaindoll-0.0.2.pom

plaindoll-0.0.2.jar

⚙️ Задание 8: Миграция конфигурации TeamCity
Цель: Перенести конфигурацию сборки в репозиторий (Configuration as Code).

Выполнено:

Создана директория .teamcity/ в корне репозитория

Реализована конфигурация на Kotlin DSL:

text
.teamcity/
├── settings.kts          # Основные настройки проекта
└── buildTypes/
    └── Build2.kt        # Конфигурация сборки
🌿 Задание 9: Создание feature-ветки
Цель: Создать ветку для разработки новой функциональности.

Выполнено:

bash
git checkout -b feature/add_reply
💬 Задание 10: Новый метод Welcomer
Цель: Добавить метод, возвращающий реплику с упоминанием "hunter".

Выполнено:

java
public class Welcomer {
    // ... существующие методы
    
    public String getHunterReplica() {
        return "A hunter is a hunter, even in a dream.";
    }
}
🧪 Задание 11: Тест для нового метода
Цель: Дополнить тесты проверкой нового метода.

Выполнено:

java
@Test
public void welcomerSaysHunter() {
    String replica = welcomer.getHunterReplica();
    assertNotNull(replica);
    assertFalse(replica.isEmpty());
    assertTrue(replica.toLowerCase().contains("hunter"));
}
📤 Задание 12: Публикация изменений
Цель: Добавить все изменения в новую ветку и отправить в репозиторий.

Выполнено:

bash
git add src/main/java/plaindoll/Welcomer.java
git add src/test/java/plaindoll/WelcomerTest.java
git commit -m "feat: add getHunterReplica method and corresponding test"
git push origin feature/add_reply
🔄 Задание 13: Автоматическая сборка
Цель: Убедиться в автоматическом запуске сборки и успешном прохождении тестов.

Выполнено:

TeamCity автоматически запустил сборку для ветки feature/add_reply

Все 5 тестов пройдены успешно

Сборка завершилась с зеленым статусом

🔀 Задание 14: Слияние в master
Цель: Перенести изменения из feature-ветки в master через merge.

Выполнено:

bash
git checkout master
git merge feature/add_reply
git push origin master
🧹 Задание 15: Очистка master от артефактов
Цель: Убедиться, что в ветке master нет собранных артефактов.

Выполнено:

Проверено отсутствие папки target/ в git-репозитории

Обновлен .gitignore:

gitignore
target/
*.class
*.jar
pom.xml.tag
pom.xml.releaseBackup
release.properties
📦 Задание 16: Настройка сбора JAR-артефактов
Цель: Настроить TeamCity для сбора и публикации JAR-файлов.

Выполнено:

Изменены цели Maven: с clean deploy на clean package

Настроены пути артефактов: target/*.jar

Проверена публикация: TeamCity успешно находит и публикует JAR-файлы

🔁 Задание 17: Повторная сборка master
Цель: Убедиться в успешности полного цикла сборки.

Выполнено:

Запущена повторная сборка ветки master

Подтверждена успешность всех этапов:
✅ Компиляция
✅ Тестирование (5 тестов, 0 ошибок)
✅ Создание JAR
✅ Публикация в Nexus
✅ Публикация артефактов в TeamCity

🗃️ Задание 18: Очистка репозитория
Цель: Оставить в репозитории только необходимые файлы, включая настройки TeamCity.

Выполнено:

Удалены backup-файлы:

bash
rm -rf .teamcity.backup
rm -f pom.xml.backup
rm -f src/main/java/plaindoll/Welcomer.java.backup
rm -f TestHunterMethod.java
rm -rf teamcittty/
Сохранены необходимые файлы:

.teamcity/ – настройки TeamCity

pom.xml – конфигурация Maven

src/ – исходный код

.gitignore – правила игнорирования

README.md – документация

settings.xml – настройки Maven

Архитектура решения
text
GitHub Repository
    │
    ├── master branch (production)
    ├── feature/add_reply (development)
    │
    └── TeamCity CI/CD
        │
        ├── Автоматический запуск сборки при пуше
        ├── Maven сборка (clean compile test package)
        ├── JUnit тестирование (5 тестов)
        ├── Создание JAR артефакта
        ├── Публикация в Nexus
        └── Публикация артефактов в TeamCity
Технологический стек
Язык программирования: Java 11

Сборка: Maven

Тестирование: JUnit 4, Hamcrest

CI/CD: TeamCity с Kotlin DSL

Артефакты: Nexus Repository

Контроль версий: Git/GitHub

Конфигурация как код: Kotlin DSL для TeamCity

Результаты
✅ Полностью автоматизированный CI/CD пайплайн
✅ Конфигурация как код (TeamCity Kotlin DSL)
✅ 5 успешных тестов
✅ Автоматическая публикация артефактов
✅ Чистый репозиторий без артефактов сборки
✅ Ветвление и слияние через feature-ветки

Инструкция по запуску
Клонировать репозиторий:

bash
git clone https://github.com/sapr797/teamcittty.git
Сборка проекта:

bash
mvn clean package
Запуск тестов:

bash
mvn test
Проверка артефактов в Nexus:

URL: http://93.77.182.74:8081

Репозиторий: maven-releases

Артефакт: org/netology/plaindoll/

Статус проекта: ✅ Все задания выполнены успешно
Последняя сборка: Успешна (зеленый статус)
Количество тестов: 5, все пройдены
Артефакты: Опубликованы в Nexus и TeamCity



Для работы деплоя необходимо запустить Nexus сервер
